1) First install all the required npm packages but before that initialize the npm by using the command the npm init
2) Making the database called WikiDB(we are using in this project) using the mongoose GUI (robot 3T) or you can make it by using the terminal
3) Understand and run the code (or you can write you own code).
4) test the code using the postman 